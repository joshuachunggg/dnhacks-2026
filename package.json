{
  "name": "dnhacks-2026",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke-test.mjs",
    "fixtures:check": "tsx scripts/check-fixtures.ts",
    "swift:check": "swift scripts/check-fixtures.swift && swiftc -parse-as-library apps/ios/EngineeringRequest.swift scripts/check-engineering-request.swift -o /tmp/check-engineering-request && /tmp/check-engineering-request && swiftc -parse-as-library apps/ios/SpatialCaptureRequest.swift scripts/check-spatial-capture-request.swift -o /tmp/check-spatial-capture-request && /tmp/check-spatial-capture-request && swiftc -parse-as-library apps/ios/SpatialCaptureRequest.swift apps/ios/LocalSpatialArtifactStore.swift scripts/check-spatial-artifact-store.swift -o /tmp/check-spatial-artifact-store && /tmp/check-spatial-artifact-store",
    "server:test": "tsx --test apps/server/src/*.test.ts",
    "check": "pnpm lint && pnpm typecheck && pnpm smoke && pnpm fixtures:check && pnpm swift:check && pnpm server:test"
  },
  "dependencies": {
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.4",
    "tailwindcss": "^4",
    "tsx": "^4.23.13",
    "typescript": "^5"
  },
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=20"
  }
}
